add_library(stellar_maintenance STATIC src/maintenance.cpp)
target_include_directories(stellar_maintenance PUBLIC include)
target_link_libraries(stellar_maintenance PUBLIC stellar_engine stellar_json PRIVATE Bcrypt Ole32)
add_library(stellar_windows_maintenance STATIC src/windows_platform.cpp)
target_link_libraries(stellar_windows_maintenance PUBLIC stellar_maintenance PRIVATE Shell32 Ole32 Advapi32 Rstrtmgr)
set(STELLAR_SETUP_RELEASE_MANIFEST "" CACHE FILEPATH "Pinned cooked release manifest for offline setup")
set(STELLAR_SETUP_MANIFEST_SHA256 "")
if(STELLAR_SETUP_RELEASE_MANIFEST)
  if(NOT EXISTS "${STELLAR_SETUP_RELEASE_MANIFEST}")
    message(FATAL_ERROR "Pinned setup release manifest is missing")
  endif()
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${STELLAR_SETUP_RELEASE_MANIFEST}")
  file(SHA256 "${STELLAR_SETUP_RELEASE_MANIFEST}" STELLAR_SETUP_MANIFEST_SHA256)
endif()
configure_file(setup_pin.hpp.in generated/setup_pin.hpp @ONLY)
configure_file(windows_version.rc.in generated/setup_version.rc @ONLY)
foreach(target StellarContinuumSetup StellarContinuumUninstall)
  add_executable(${target} WIN32 src/main.cpp windows.manifest "${CMAKE_CURRENT_BINARY_DIR}/generated/setup_version.rc")
  target_include_directories(${target} PRIVATE "${CMAKE_BINARY_DIR}/generated" "${CMAKE_CURRENT_BINARY_DIR}/generated")
  target_compile_definitions(${target} PRIVATE UNICODE _UNICODE)
  target_link_libraries(${target} PRIVATE stellar_windows_maintenance Comctl32 Dwmapi Uxtheme)
  set_target_properties(${target} PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/installer-tools")
endforeach()
target_compile_definitions(StellarContinuumUninstall PRIVATE STELLAR_UNINSTALLER=1)
if(BUILD_TESTING)
  add_executable(StellarContinuumSetupReview WIN32 src/main.cpp windows.manifest "${CMAKE_CURRENT_BINARY_DIR}/generated/setup_version.rc")
  target_compile_definitions(StellarContinuumSetupReview PRIVATE UNICODE _UNICODE STELLAR_INSTALLER_UI_TESTS=1)
  target_include_directories(StellarContinuumSetupReview PRIVATE "${CMAKE_BINARY_DIR}/generated" "${CMAKE_CURRENT_BINARY_DIR}/generated")
  target_link_libraries(StellarContinuumSetupReview PRIVATE stellar_windows_maintenance Comctl32 Dwmapi Uxtheme)
  set_target_properties(StellarContinuumSetupReview PROPERTIES RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/installer-review")
  add_executable(stellar_maintenance_tests ../native-tests/maintenance_tests.cpp)
  target_link_libraries(stellar_maintenance_tests PRIVATE stellar_maintenance)
  add_test(NAME engine_windows_maintenance COMMAND stellar_maintenance_tests "${CMAKE_BINARY_DIR}/maintenance-tests")
  add_executable(stellar_windows_maintenance_tests ../native-tests/windows_maintenance_tests.cpp)
  target_link_libraries(stellar_windows_maintenance_tests PRIVATE stellar_windows_maintenance)
  add_test(NAME engine_windows_maintenance_os COMMAND stellar_windows_maintenance_tests "${CMAKE_BINARY_DIR}/maintenance-os-tests")
endif()
