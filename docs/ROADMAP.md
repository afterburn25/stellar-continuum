# Engineering roadmap — native C++ branch

Audited 2026-09-20. **Future work is not completed work.** Existing foundations
are named explicitly below; statuses apply to the full proposed capability.
The [architecture](ENGINE_ARCHITECTURE.md), [current state](PROJECT_STATE.md) and
[known issues](KNOWN_ISSUES.md) govern this branch. The previous roadmap is
preserved as [historical design/status text](history/ROADMAP.pre-native-sync-20260920.txt).

## Immediate reliability work

1. Make a clean native checkout build reproducibly and restore the test/CI
   contract. Review generation fingerprint drift semantically; repair Python
   fixture/catalog assumptions; remove retired CI target references.
2. Investigate the broad developer smoke's supplied-planet-map assertion with
   canonical material identity, actual texture size and captured visual evidence.
3. Run a combined late-game campaign workload with AI, fleets, research, combat
   and autosave. Report per-phase time, memory and save latency, not only FPS.
4. Preserve art sharpness while improving lossless cook fallback rate, streaming
   and package size. Every visual change needs source/cooked/in-game comparison.
5. Validate the next exact-base update against a fresh install and a copied
   real campaign. Keep installed saves untouched and publish only update installers.

## Next engine expansion: 1–30

| # | Capability | Current status / existing foundation | Next deliverable and acceptance boundary |
| --- | --- | --- | --- |
| 1 | Unified Entity/World System | PARTIALLY IMPLEMENTED — generational `EntityRegistry`, engine `world.cpp` typed store, and `campaign_world_projection` bridging authoritative `FreshCampaignState` containers to typed entities (namespaced legacy ids, tag components, honest hierarchy, incremental `sync_campaign_world` reconciliation with stable handles; `campaign_world_projection_tests` covers stale handles + snapshot identity); domain containers remain authoritative | Graduation criteria for any simulation-side adoption (parity, saves, determinism, privacy); no blanket rewrite |
| 2 | Simulation Scheduler + Simulation LOD | IMPLEMENTED BUT NEEDS POLISH — `simulation_scheduler.cpp` tier cadence + `simulation_executor.cpp` tasks/dependencies/dirty+event wakeups/budgets/JobSystem waves/per-domain stats (`simulation_executor` + `simulation_scale_250…5000` tests/benchmarks); `GalaxySimulationStepCoordinator` drives all 12 Core campaign phases as dependency-chained executor tasks at the Active tier | Per-phase tier demotion (LOD policy) under a seeded parity oracle — determinism and privacy must hold before cadence changes |
| 3 | Full Job/Threading System | PARTIALLY IMPLEMENTED — `JobSystem` with priorities, cooperative cancellation, `submit_graph` dependency graphs and per-tag stats + `job_system_tests`; consumers: image preparation, audio director, territory overlay, campaign session, engine-shell scene3d texture decode (async `scene3_tex` — entities bind untextured until the decode job publishes, decode errors surface once via the tool status line) | Work-stealing/affinity; owner-safe publication audited per consumer |
| 4 | Render Graph | PARTIALLY IMPLEMENTED — engine `render_graph.cpp` schedules the native client's scene3d→tonemap pass chain per frame (real consumer) + `render_pipeline_tests` | Wider pass adoption and measured before/after; CPU submission stays bounded |
| 5 | GPU-Driven Rendering | PLANNED — CPU batching/culling and cached GPU resources exist | Measured indirect/instanced path, GPU culling and feature fallback without changing object identity |
| 6 | Virtual/Streaming Texture System | PARTIALLY IMPLEMENTED — `TextureStreamer` owns residency under a runtime-tunable byte budget (`Window::set_scene3d_texture_budget`), screen-footprint desired-mip selection (anisotropic materials keep full chains), per-mip partial-tail admission under pressure, pinned-fallback pop-in, eviction accounting (`streamed_evicted_bytes`/`streamed_fallbacks`); fault/budget/LOD GPU-tested; incremental promotion verified (footprint growth re-uploads the finer mip prefix — `mip-ring-close` asserts the +1 upload) | Driver-side sparse/virtual texturing not attempted; desired-mip uses bounding-sphere footprint, not per-UV density |
| 7 | Shader Library + Shader Cache | PARTIALLY IMPLEMENTED — source/embedded SPIR-V hash contract | Variant keys, compilation/cache lifecycle and invalidation; offline shipping remains supported |
| 8 | General Particle/VFX Framework | IMPLEMENTED BUT NEEDS POLISH — engine `VfxSystem`: shared `EmitterDefinition`s, deterministic pools, lifetimes, entity attachments, per-instance LOD rate fade, and a global particle budget (RuntimeHost defaults to 64k); `render_pipeline` covers determinism/LOD/budget | Adopt the framework for authored game VFX (client eruption art remains bespoke); no return to rejected blurry flare volume |
| 9 | Physics Layer | PARTIALLY IMPLEMENTED — analytic orbit, kinematics and continuous primitive queries | Unit/frame/time contracts and reusable collision broadphase; define scope before N-body or rigid-body expansion |
| 10 | Spatial Query Framework | PARTIALLY IMPLEMENTED — point/region/3D indices and parent chains | Unified typed query/lifetime API and batched result ownership; scale/parity tests |
| 11 | Navigation & Route Engine | PARTIALLY IMPLEMENTED — Core lanes/reach/fuel/freight planning | Reusable routing jobs/cache invalidation with observer and resource constraints |
| 12 | Knowledge/Fog-of-War Engine | PARTIALLY IMPLEMENTED — authoritative Core observation/knowledge | General visibility/provenance queries retaining privacy, persistence and safe Developer boundaries |
| 13 | Generic Economy/Resource Framework | IMPLEMENTED BUT NEEDS POLISH (engine layer) — `economy_catalog.cpp`: `ResourceSpec`/`RecipeSpec` metadata, strict validation (duplicates, unknown refs, cycles, unreachable chains), `EconomyGraph` dependency queries, `analyze_economy` bottleneck/reserve/import diagnostics, `to_runtime_recipe` bridge into `ResourceNetwork`; Core credit/resource/industry/biology services remain authoritative | Author a data-driven Core economy catalog on the framework (substitution runtime, catalyst/labor/facility gating via colony framework); preserve current rule authority |
| 14 | Event Bus | IMPLEMENTED — `EventBus` typed synchronous pub/sub | RAII `Subscription` lifetimes (safe unsubscribe mid-dispatch), registration-order dispatch, owner-thread enforcement (simulation owner only — violations throw), `defer()`/`drain()` queue ordered by (tick, sequence), delivery stats. Real consumer: `MissionRuntime` publishes `MissionEffectEvent` through it and the engine shell's Missions debugger subscribes over a live bus |
| 15 | Mission/Event Framework | PARTIALLY IMPLEMENTED — engine `mission_graph.cpp` (JSON triggers/stages/timers/choices, `MissionEffectEvent` bus payloads, serialize/restore) + `mission_graph_tests`; consumed by the engine shell's Missions debugger tool over a live `EventBus` | A real game mission domain (Core-authored narrative/anomaly chains) — gated on Core authority and observer filtering; no hidden UI-only mission state |
| 16 | Advanced Save System | PARTIALLY IMPLEMENTED — Player17 DTO/JSON, atomic backup/recovery | Versioned incremental snapshots/chunks, migration suite and bounded large-save memory |
| 17 | Deterministic Replay | PARTIALLY IMPLEMENTED — engine `replay.cpp` command journal consumed by the native client (`--record`/`--replay`, verified `diverged:false`); section-labeled checkpoints localize divergence (`document_section_checkpoints`/`verify_checkpoint_sequence`) and the client attests recorded seed/build_id/game_version against the running build | Checkpoint cadence/coverage tuning and replay tooling UX |
| 18 | Crash Reporter | PARTIALLY IMPLEMENTED — real local bounded logs, reports and Windows dumps | Symbol-matched diagnosis/bundle UX, better fault coverage; no implicit upload |
| 19 | Profiler | PARTIALLY IMPLEMENTED — phase timers, per-thread spans, counters/gauges, retained frame ring, frame capture + scenario comparison | `ProfileCapture` round-trips `export_json` captures (parse/to_json); `compare_captures` diffs two captures into per-span mean deltas — the shell Profiler tool CAPTURE A/B, SAVE/LOAD (`<exe>/profiler_captures/`) renders the largest deltas. GPU timeline and lower-perturbation sampling remain open |
| 20 | Memory Tracking | IMPLEMENTED BUT NEEDS POLISH — `MemoryTracker` subsystem registry (current/reserved/high-water per name) consumed by the client (`planet-materials`) and now by `Window::draw` itself: scene3d VRAM (textures/meshes/render-targets) and 2D image/text caches report every frame — GPU-verified parity between attributed bytes and renderer residency | Allocator-tag census and integrated GPU timeline remain open; attribution is draw-paced, not allocation-paced |
| 21 | Proper Input System | IMPLEMENTED — `InputMapper` action contexts + rebinding + persistence | Stacked contexts (exclusive fall-through), Button/Axis1D/Axis2D actions, chords, gamepad axes with held-value semantics, runtime `rebind()`, `bindings()` inspection for rebind UIs, and `save_contexts()` round-tripping the rebound map through the `load_contexts` JSON schema — `RuntimeHost` feeds every normalized event through it and projects override via an input-map JSON. Device policy/focus-capture tests remain open |
| 22 | Audio Engine | PARTIALLY IMPLEMENTED — SDL output, decoder, music/SFX/voice settings; device-fault recovery (bounded 5 s retry rebuilds the output device and resumes music — `native_audio_director` injected-fault test) | Incremental streaming and buses/spatial audio |
| 23 | Animation System | PARTIALLY IMPLEMENTED — `FloatCurve`/`Timeline`/`AnimationPlayer` + scene-authored entity tracks | Keyframed scalar curves with easing, looping timelines with event markers, and `AnimationPlayer` playhead lifecycle (play/pause/speed/seek, crossed-event emission, once-clamp). Scene documents declare `animations` clips (channels: x/y/w/h/vx/vy/opacity/rotation/spin/tintRGB) referenced by entity `anim` — `RuntimeHost` steps each `AnimTimeline` in sim time (deterministic under --fixed-hz), forwards crossed events via `on_anim_event`, and snapshots/restore the playhead. Skeletal/transform blending and LOD contracts remain open |
| 24 | Advanced UI Framework | PARTIALLY IMPLEMENTED — shared skin/text/layout and native workspaces | Focus/navigation, reusable widgets/layout/accessibility and view-model boundaries |
| 25 | Localization | PARTIALLY IMPLEMENTED — engine `LocalizationTable`/`LocalizationService` (fallback chain, `{n}`/`{name}` formats, plural selection, reload) drives every live player-facing surface via shipped `Data/locale/<id>.json` catalogs (~1,400 keys each, staged into packages): startup/setup, diplomacy, research, fleet, construction, inspection, economy, supply, colony/planetary, system/battle/shipyard/overview, notifications, HUD, globe, phenomena, controlled-assets, settlement, pause menu, settings panels, voice subtitles, and producer-side status/preset/projection strings. Second shipped locale landed: full German `de.json` with English fallback, persisted `locale` preference + LANGUAGE cycler in General Settings, shipped-catalog key/placeholder parity asserted in tests | Additional shipped locales follow the same pattern; layout-expansion testing against long translations open; developer panels and diagnostics stay English |
| 26 | Accessibility | PARTIALLY IMPLEMENTED — engine `AccessibilitySettings` plus full client adoption: General Settings persists `reduceMotion` (decorative animation), `reduceFlashing` (polar-pulse luminance hold), `highContrast` and `colorBlind` (global DrawList passes in `native_ui_theme`: dim-text snap + Machado daltonization over every CPU-side color field), `interfaceScale` (user multiplier over viewport scale in `NativeUiLayout`) — all with settings and transform tests | Keyboard/assistive-technology requirements (focus traversal, screen-reader contracts); per-surface text scaling beyond the global multiplier |
| 27 | Platform Layer | PARTIALLY IMPLEMENTED — Windows/SDL services, paths/leases and settings | Explicit portable interfaces and platform tests; Linux/macOS support is not yet established |
| 28 | Steam Integration Layer | PARTIALLY IMPLEMENTED — engine `PlatformServices` facade + null backend + `package_platform_tests`; client owns the facade and reports backend status in diagnostic bundles | A real Steamworks backend for lifecycle/cloud/achievements after offline save/release contracts stabilize; no executable plugin trust |
| 29 | Mod Architecture | IMPLEMENTED — `PackageRegistry` + `mods/` scan + save attestation | Namespaced ids, priority overrides, semver dependency resolution, manifest validation and protected base namespaces are in the package system; world saves write a `<save>.packages.json` sidecar and the runtime host verifies it on load (missing/version-mismatched packages report via RuntimeDiagnostics). Executable plugins remain intentionally untrusted — no DLL/script loading is planned |
| 30 | Engine Editor / Stellar Tools | PARTIALLY IMPLEMENTED — cooker/import CLI and Developer inspectors; `stellar-engine.exe` standalone tools host (engine-only link; Projects/Dashboard/Assets/Profiler/Localization/Missions/Physics/Jobs/… tools, full project lifecycle create→author→cook→build→test→run→package against the exported engine SDK) and `stellar-editor.exe` native C++23 editor host ship beside the client — authoritative world-assembly pipeline, galaxy + system-orbit workspaces, body inspection, searchable system list, system/body name/note/bookmark annotations with document-level undo/redo, editable project name, atomic project save/load, multi-file project directories with embedded assets, trait overrides (anomaly/rare-resource/pre-warp AUTO/YES/NO), and numeric body overrides (radiusEarth/massEarth/orbitAu with derived effective gravity) round-tripping through the project codec | Save-As path picker / recent files; further property coverage (stellar-orbit overrides exclude moons by design); transform gizmos; unified tool document lifecycle |

## Dependency and completion policy

Prioritize ownership, scheduling, jobs, spatial queries and diagnostics before
broad renderer/editor systems. Build save/replay compatibility alongside World
and scheduling changes, not afterward. Drive renderer/streaming investment from
measured workloads; preserve the supplied artwork and stable frame behavior.

A capability is done only when its reusable owner, API, consumers, tests,
performance/save impact and limitations are documented in the
[capability registry](ENGINE_CAPABILITIES.md). Every significant workstream leaves
a GitHub commit and updated handoff. A design document or requested feature
alone is never evidence of implementation.
