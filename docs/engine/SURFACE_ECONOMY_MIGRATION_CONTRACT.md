# Surface building output and grid storage migration

Frozen interface: `core/include/stellar/core/surface_economy.hpp`. Port the preserved `src/Game/Simulation/Construction/SurfaceConstruction.cs` catalog, functional-family/essential-priority helpers, building capacity, construction-stage projection, district specialization, GetOutput, and AdvancePowerStorage. Reuse the existing native Colony/SurfaceBuilding without altering their stored fields.

Preserve all 14 authored definitions and descriptions, optional upgrade/research references, numeric defaults and float footprint radius. Costs stay the reference ledger units. This gate does not authorize buildings, advance construction, apply research, fund upkeep, degrade/repair condition, or advance a whole economy tick.

Allocation follows operating priority descending, essential services descending, then ID; stable ties preserve insertion order. Preserve 45% workforce participation, 2 baseline power, strict condition > .15 eligibility, unstaffed upkeep and workforce demand, efficiency .5+.5*condition, unsupplied demand, greedy power allocation and specialization selection from completed/enabled/functional buildings (not just staffed ones). Preserve storage totals from all completed batteries, but only staffed operational batteries charge/discharge. Retain interval-dependent discharge/charge caps and 90% efficiency. Stored values advance in ascending building ID order; preview does not mutate the colony. Set-valued output IDs compare as sets against C# while native diagnostics remain deterministically ordered. Duplicate IDs are not made into a new valid-save convention.

Construction stage boundaries (.15/.40/.75/.95), outpost/hub capacity (8/16/32/64) and exact district descriptions are preserved. No foreign C# enum ordinal assumptions. Finite interval validation fails cleanly. Unknown building types fail with a useful native message where the old valid-state-only allocator could dereference null; test this diagnostic difference explicitly, without pretending it is identical exception text.

Reference fixtures come from actual original C# calls: all catalog fields, complete/incomplete/disabled/condition-threshold buildings; workforce scarcity and priorities/ties; power scarcity; each active specialization and mixed ties; partially damaged output, habitat cap, real food/water/housing/cargo; batteries empty/full/partial/multiple, inactive storage totals, charge/discharge saturation, intervals and repeated depletion/recharge, zero-day nonmutation and invalid intervals. Cover construction-stage exact boundaries and all capacity levels, output immutability, full state preservation except stored energy. Include an integration fixture using native output's explicit sustenance projection with the already-ported capacity/reserve calculations; original reference outputs remain authoritative.

Ownership: implementation worker owns `core/src/surface_economy.cpp` and C# oracle/fixture; test worker owns native test consumer. Coordinator owns this header, CMake/workflow/export integration and documentation. Do not edit preserved gameplay source. Headless parity is not graphical or playable-game parity.
