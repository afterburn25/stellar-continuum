# Stellar Engine migration status

Engine 0.1.35 integrates the actual Adaptive Research campaign owner (075), declares and packages all 70 authored research JSON files (076), and adds `--simulate-adaptive-campaign` to the exported native diagnostic host (077). Isolated strict Debug/Release passed the 15-row complete composition fixture and the positive, deterministic, missing-data, corrupt-data and output-preservation host checks. Detailed phase tracing is opt-in; ordinary game updates copy no diagnostic vectors. Combined maintained validation passed 84/84 CTest (`work/native-035-testing.log`) and 28/28 Python (`work/native-035-python-final.log`), including the three retained adaptive host recovery tests. The maintained actual-source generator reproduced the fixture byte-for-byte. Exact committed export follows. Player wrapper17/galaxy16 saves, tactical combat continuation and the native graphical client remain open. Metadata/core persistence078 and tactical-state validation079 are being developed independently.

Exact committed 0.1.34 export `2041479f7e927843f4c04cbca52c8876f9991f86` passed83/83 CTest and20/20 Python. Package `Builds/Windows/StellarContinuum-windows-benchmark-2041479f-20260913T182806353168Z` reports sourceDirty=false, seven sealed runtime files and all nine relocation/recovery flags true (`work/native-034-clean.log`). All six GitHub workflows passed, including native34774515729 and screenshot34774515627.

Engine 0.1.34 adds shared Diplomacy exploration/combat/maintenance runtime adapters (073) and bounded fleet-power observation/sensor intelligence (074). Isolated strict Debug/Release passed22 runtime source rows and39 fleet source rows plus one explicitly source-only null-target boundary. Root review corrected ANY-identified contact handling, float-selector/double-sum power arithmetic, NaN propagation and bounded value-equality eviction; the final maintained run passed83/83 CTest and20/20 Python (`work/native-034-testing-fixed.log`). The first maintained replay exposed a test-only fixture schema assertion; it was corrected without altering production or fixture bytes. Exact committed export follows. Combat preview and issuance share the same political state; unknown enemy power stays unknown without an observation. Combat intelligence is appended to FreshCampaignState without rearranging existing aggregate fields. Actual integrated Adaptive campaign075 and declared research-data export076 continue in isolation. The exported host still runs legacy research; player wrapper17/galaxy16 persistence and native graphics remain open.

Exact committed 0.1.33 export `e2cc89956ffea4df02659053c7b566fb6fd38cac` passed81/81 CTest and20/20 Python. Package `Builds/Windows/StellarContinuum-windows-benchmark-e2cc8995-20260913T174822163007Z` reports sourceDirty=false, seven sealed runtime files and all nine relocation/recovery flags true (`work/native-033-clean.log`). All six GitHub workflows passed, including native34772520986 and screenshot34772520992.

Engine 0.1.33 adds Diplomacy clock/lifecycle/maintenance (071) and observer/campaign commands (072). Strict Debug/Release passed40 lifecycle source rows plus one explicit native duplicate-key boundary, and46 observer/campaign source rows. Source/production review preserves observer privacy, first-failure/status order, aging and proposal partial effects, ceasefire/access termination, cadence and known-world claim/warning restrictions. Maintained validation passed81/81 CTest and20/20 Python (`work/native-033-testing.log`); exact committed export follows. Runtime exploration/combat/diplomacy composition073 and fleet scanner intelligence074 continue. The exported host still uses retained legacy research; actual Adaptive host, player wrapper17 recovery and native graphics remain open.

Engine 0.1.32 adds authoritative Diplomacy commands (069) and strict current-save snapshot validation (070). Isolated Debug/Release passed57 command rows and127 native invariant rows, with one explicit null-source boundary. Review preserves hidden-state rejections, proposal side-effect order, checked-counter limits and first-error validation order. A final nonempty owned-snapshot test initially used a wrong helper member and failed compilation; the test-only reference was corrected without changing production or fixture data. Maintained validation passed79/79 CTest and20/20 Python (`work/native-032-testing-fixed.log`); exact committed export `5892a0aecbc9fe038d84bc8f9b3b373814219598` passed the same79/20 checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-5892a0ae-20260913T173022382727Z` has `sourceDirty=false`, seven sealed runtime files and all nine relocation/recovery flags true (`work/native-032-clean.log`). Lifecycle/maintenance071 and observer/campaign commands072 continue; actual Adaptive Research/Diplomacy host composition, player wrapper17 saves and native graphics remain open.

Engine 0.1.31 integrates timed Adaptive Research campaign progression/facilities (066) and low-level Diplomacy state, observer views and historical recovery (068). Final isolated Debug/Release passed31 research rows and35 diplomacy rows (34 parity plus one explicit checked-counter boundary). Review preserves source ordering, funding NaN/overflow behavior, fresh versus recovered transient state, stable duplicate contact order, bounded storage failure/eviction and detached ownership. Combined maintained validation passed77/77 CTest and20/20 Python (`work/native-031-testing.log`); exact committed export `ed1ea3916f7d2914aa5c50d206126b7ebe8b7c5b` passed the same77/20 checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-ed1ea391-20260913T170025989251Z` has `sourceDirty=false`, seven sealed runtime files and all nine relocation/recovery flags true (`work/native-031-clean.log`). All six0.1.31 GitHub workflows passed, including native34770081118 and screenshot34770081160. Authoritative Diplomacy commands069 and strict current-save validation070 continue in isolation. The exported host still runs retained legacy research; full Adaptive Research/diplomacy composition, player wrapper17 persistence and native graphics remain open.

Engine 0.1.30 integrates Adaptive Research campaign ownership/factory/schema2 recovery (064), funding policy and commands (065), and authoritative construction/shipbuilding prerequisite views (067). Final isolated Debug/Release passed23 campaign rows,47 funding rows and33 capability rows. The campaign checker now has clean failure diagnostics, canonical input fingerprints, retained snapshot ownership and full immediate/deferred funding-sequence checks. Combined maintained validation passed 75/75 CTest and 20/20 Python checks (`work/native-030-testing-embedded.log`); exact committed export `2432012a7a1681874c6b9b03617ae1fd247cfedc` passed the same75/20 checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-2432012a-20260913T161603704614Z` has `sourceDirty=false`, seven hashed runtime files and all nine relocation/recovery flags true (`work/native-030-clean.log`). All six engine0.1.30 workflows passed, including native34767887436 and screenshot34767887313. The initial link failed with LNK1103 corrupt debug information. Debug and RelWithDebInfo now embed full compiler symbols per object (/Z7), keeping linker symbols while avoiding that shared compiler-PDB dependency. No simulation assertion or test was suppressed. Timed campaign progression/facilities (066) and Diplomacy data/state (068) continue in isolation. The exported host still runs legacy research; integrated Adaptive Research, strict player saves and native graphics remain open.

Engine 0.1.29 integrates standalone schema4 foreign-technology recovery (062) and schema5 outcome-history recovery (063), with one shared internal enum converter. Retained source evidence covers 25 foreign cases plus a separate nonfinite serialization boundary, and 51 outcome rows with explicitly different parser/overflow boundaries. Each isolated Debug/Release matrix passed. The combined maintained build passed 72/72 CTest and 20/20 Python checks (`work/native-029-testing.log`). Exact committed export `de6c750c366d1bbf2fb4423ddf1f9db18da6dcf5` passed all72/72 CTest and20/20 Python checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-de6c750c-20260913T151850838371Z` has `sourceDirty=false`, seven hashed runtime files and all nine relocation/recovery flags true (`work/native-029-clean.log`). Separate-machine certification remains open. Campaign owner/factory/snapshot (064) and authoritative funding commands (065) continue in isolation; campaign progression, actual exported Adaptive Research, diplomacy, player persistence and native graphics remain open.

Engine 0.1.28 integrates foreign-discovery coordination (059), a stable owning research runtime (060), and standalone schema-3 pressure/agenda recovery (061). The combined maintained build passed 70/70 CTest and 20/20 Python checks (`work/native-028-testing.log`). Final review strengthened successful restore/revision comparisons and exposed an incorrect nested-schema error; DTO presence metadata now preserves schema validation before nullable record access. Corrected isolated Debug and Release each passed 53 schema3, 81 schema2 and 73 schema1 rows. All five affected maintained checks then passed (`work/native-028-final-focused.log`). Exact committed export `a9610c7426d8fdf5c6f493a2b28b154fe7081b6f` passed all 70/70 CTest and 20/20 Python checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-a9610c74-20260913T144010986128Z` has `sourceDirty=false`, seven hashed runtime files and all nine relocation/recovery flags true (`work/native-028-clean.log`). All six GitHub workflows passed for 0.1.28, including native 34763184847 and screenshots 34763184841. Separate-machine certification remains open. Schema4 foreign metadata and schema5 outcome history recovery continue in isolated gates062/063, followed by authoritative campaign funding/progression. The existing playable baseline remains intact; this is a headless engine checkpoint, not native graphics or player-save compatibility.

Engine 0.1.27 integrates foreign technology intake/support (057) and deterministic research outcomes (058). The combined maintained build passed 67/67 CTest and 20/20 Python checks (`work/native-027-testing.log`). Foreign evidence covers 29 sequential commands, 17 matched malformed catalog cases plus one source-only duplicate-key boundary, 8 state operations and 17 native lifetime/alias probes. Outcomes cover 82 actual-source rows, including all nine outcome kinds, portable SHA-256, history trimming and partial failures, with one explicitly different numeric-parser boundary. Review fixed JSON/error sequencing, history trimming across nodes, fixed-order risk arithmetic and distinct field intersections. Exact committed export `145593ae587316addfd3f1abf3b6d46fe26dfd7d` passed all 67/20 checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-145593ae-20260913T135004496271Z` has `sourceDirty=false`, seven hashed runtime files and all nine relocation/recovery flags true (`work/native-027-clean.log`). All six GitHub workflows for this exact commit passed, including native CI 34760791808 and screenshot CI 34760791806. Discovery (059), stable StrategicRuntime ownership (060) and standalone schema3 support recovery (061) continue in isolation. Current player persistence is wrapper17 around galaxy16; full campaign wiring, diplomacy and native graphics remain open.

Engine 0.1.26 integrates pressure signals/support (055) and visible research agendas (056). The combined maintained build passed 65/65 CTest and 20/20 Python checks (`work/native-026-testing.log`). Pressure evidence includes 59 rules,14 matched malformed/catalog cases plus one source-only parser boundary, 12 low-level operations and 20 runtime commands. Agenda evidence contains 60 actual-source rows including 14 malformed inputs. Native lifetime probes cover independent support, copy/move identity, exact object-address reuse, expired cleanup and owned inputs. Review fixed sparse priority reinsertion order, lazy state/error sequencing, and general numeric diagnostic formatting. Exact committed engine 0.1.26 export `84d7370225a6b6b234707890ad8e1cb3c078d047` passed 65/65 CTest and 20/20 Python checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-84d73702-20260913T125937489845Z` has `sourceDirty=false`, seven hashed runtime files, and all nine relocation/recovery validation flags true. Evidence: `work/native-026-clean.log`. All six GitHub workflows for this commit passed, including native CI 34758146962 and screenshot CI 34758146958. This remains a headless foundation, not a native playable release or clean-machine certification. Foreign intake (057) and deterministic research outcomes (058) continue in isolation; integrated Adaptive Research, diplomacy, save-v16 and graphics remain open.

Clean engine 0.1.25 is committed as `aa3eaa834bd9c8570bde4d70b4265f47da1a5d2a`. Package `Builds/Windows/StellarContinuum-windows-benchmark-aa3eaa83-20260913T120628476660Z` verifies `sourceDirty: false`, 63/63 CTest and 20/20 Python checks, seven sealed runtime files and all relocated launch, initialization, recovery and retained campaign simulation checks. The exact export log is `work/native-025-clean.log`. All six workflows passed for this exact commit, including native `34756054539` and screenshots `34756054510`. All six workflows for the preceding clean 0.1.24 passed. This remains a headless engine checkpoint, not a graphical player build.

Engine 0.1.17 integrates timed colony establishment/commands, strategic intent/providers, observer-local planning, read-only combat readiness and prototype technology capability mappings. Validation documents identify actual C# invocation counts separately from forwarded cases, source-only null observations and native safety boundaries. The clean export log is `work/native-017-clean.log`.

Engine 0.1.18 integrates strategic own-state construction (035), matched combat commands (036), scheduled director/runtime composition (037), ordered campaign stepping and combat outcomes (038), and exact-own fleet status (039). The final tightened campaign comparison passed strict Debug and Release replays and all clean export checks. See `CAMPAIGN_COORDINATOR_VALIDATION.md` for development transcripts and hashes; `work/native-018-clean.log` is the exact committed export.

Engine 0.1.19 integrates the maintained multi-step campaign benchmark (041), deterministic diagnostic output, no-overwrite protection, relocated simulation checks and four-size export measurements. Development validation passed 49/49 CTest and 20/20 Python checks. Longer 250/2500-system runs each completed 1000 steps across two repeats with actual allocation/construction output and identical final state. See `CAMPAIGN_SIMULATION_BENCHMARK_VALIDATION.md`; the exact committed export is `work/native-019-clean.log`.

Engine 0.1.20 integrates the remaining military, deployment, settlement, freight and civilian-recovery coordinator commands (040). The retained source fixture has eight scenarios and 42 sequential commands, including nonempty plans, successful settlement orders, duplicate-ID re-resolution and partial failure/retry. Its local export passed 50/50 CTest and 20/20 Python checks, but recorded `sourceDirty: true` because a draft compiler emitted a root PDB during the build. The identified artifact is now contained in its ignored draft directory. The package is development evidence, not a clean checkpoint. Its remote native job also exceeded the 300-second compilation limit; the following checkpoint gives compilation a bounded 900 seconds and leaves runtime check limits unchanged. See `CAMPAIGN_COMMAND_VALIDATION.md`, `CAMPAIGN_COMMAND_CASE_MATRIX.md`, and the exact package details in `HANDOFF.md`.

Engine 0.1.21 integrates the immutable Adaptive Research catalog (042), facility/progress catalogs (043), and sparse civilization state (044). Actual-source evidence covers 103 catalog cases plus three source-only observations, 28 policy cases with 2,969 facility queries, and 74 state commands plus seven constructor cases. Its exact committed export passed 53/53 CTest and 20/20 Python checks with the version verified. Cold compilation now receives a bounded 900 seconds after the remote 0.1.20 timeout.

Engine 0.1.22 integrates applicability (045), standalone research snapshots (046), and eligibility (047): 26 applicability source rows, 73 snapshot rows plus one separate null-string boundary, and 415 eligibility cases across 370 canonical nodes plus six separate null cases. The combined maintained build passed 56/56 CTest and 20/20 Python checks. Its exact committed export verified 0.1.22 and the clean manifest with all 56/20 checks. Snapshot validation is limited to schema-1 research payloads; source-only null strings and arbitrary combined malformed JSON remain documented compatibility limits.

Engine 0.1.23 integrates view generation (048), runtime kernel (049) and expertise catalog/state (050). Evidence includes 20 view cases plus one separate null-state observation; 63 runtime commands across 20 scenarios and all 14 event types; all 36 expertise fields/24 institutions, four vector queries, 35 state commands and 26 retained malformed-input cases. The combined maintained build passed 59/59 CTest and 20/20 Python checks. Its exact committed clean export passed all 59/20 checks. The earlier package ending `20260913T104243841856Z` is development evidence only because documentation changed during packaging; see HANDOFF.md.

Authoritative composition and AuthorityInputs (053) are integrated after the 0.1.24 checkpoint and passed the focused maintained replay: eight profiles, 29 sequential commands and native move/alias checks (`work/native-053-authority-focused.log`). Schema-2 research recovery (054) is integrated with 81 actual-source cases. Pressure support with weak state identity (055) is promoted and passed four maintained focused checks; visible-only agenda planning (056) is under final review. Foreign-technology intake (057) continues in isolation. Work identifiers are not engine release versions. Integrated Adaptive Research, diplomacy, persistence and SDL3/Vulkan presentation remain open. PR #325 is draft/unmerged; the existing Godot playable baseline remains intact. Territorial PR #323 is paused with enclosed pockets unresolved.

Gate 051 readiness/service and owned expertise state attachment have passed the maintained 60/60 CTest and 20/20 Python suite (`work/native-051-readiness-testing.log`). Source evidence is 23 readiness cases, 36 sequential service commands and six focused service cases. Gate 052 starting profiles is integrated and passed its focused 1/1 CTest check; source evidence covers all eight canonical profiles and 42 boundary rows with exact retained input bytes. These gates form engine 0.1.24. Its exact committed clean export passed the combined 61/20 checks; native CI run `34754320633` also passed. The newer 053 focused evidence does not change that committed package's contents.

Engine 0.1.25 integrates calculated research authority/inputs (053) and standalone schema-2 expertise recovery (054). The combined maintained suite passed 63/63 CTest and 20/20 Python checks (`work/native-054-recovery-testing.log`). The initial link attempt omitted the Windows bcrypt dependency used by the snapshot fixture hash check; the complete Windows-only test target now links it, and the retained initial failure is `work/native-054-recovery-link-failure.log`. The exact committed 0.1.25 export also passed these checks and its clean manifest and relocation report were verified.
