# Native galaxy artwork sources

The C++ client packages these approved Stellar Continuum source images:

- `assets/visual/space/deep-field-v3.png`: the user's replacement deep-space background, supplied September 17, 2026 as `afterburn25._photorealistic_ultra-sharp_deep-space_background_49460a09-56a6-40f4-89d7-618cbcbf3657_0.png`. The packaged file is byte-identical to the supplied 2944 × 1648 PNG; SHA-256 `f18e121dd515a1ae775512e3ed7afea2fcfd370867c65dd6b3a7e970134e4cb9`. It remains the active zoomed-out galaxy overview background, separate from the regional star-map background. It replaces the earlier generated `deep-field-v2.png` in the native package. No image generation or retouching was applied to this supplied replacement.
- `assets/visual/space/spiral-galaxy-v3.png`: the user's replacement `Spiral Galaxy.png`, supplied on 2026-09-16 from `C:/Steller Continuum/Galaxy's/Spiral Galaxy.png`. The packaged PNG is byte-identical to the supplied source. The native renderer converts its near-black matte to transparency once during image preparation, preserving luminous arms and eliminating rectangular borders. Its original 1456:816 aspect ratio is retained. The backdrop fits the generated catalog's extent and scales with it; authoritative positions, physical distances, routes, ownership and discovery never come from its pixels.

- `assets/visual/space/regional-nebula-b.png`: existing approved project regional star/nebula backdrop, reused unchanged at closer galaxy-map zoom. Source role is recorded in `docs/CINEMATIC_MAP_AND_SURFACE.md`.

- `assets/visual/galaxies/`: twelve morphology-specific images supplied September 17, 2026. Six matched Stars Included / Gas-Dust Only pairs are mapped in `docs/GALAXY_ASSET_REPORT.md`. Preview cards use Stars Included; the actual map uses Gas-Dust Only behind generated systems. Irregular and Spiral pairs use the requested widescreen edits, with full provenance and final prompts in `docs/GALAXY_WIDESCREEN_ART.md`. Other pairs retain the supplied artwork through aspect-preserving import. No population-specific source variants were supplied.

Prompts and generation filenames for earlier project artwork are retained in `docs/CINEMATIC_ASSET_PROVENANCE.md`; the replacement overview background and galaxy artwork were supplied directly by the user. The user's Stellaris screenshots are visual references and are not shipped. The procedural generator remains authoritative, with cached gas-layer masks constraining the visual footprint.

These sixteen PNGs and this source note are declared by `export/native-galaxy-art-assets.json`; packaging verifies their paths and SHA-256 fingerprints.

- `assets/visual/space/star-background.png`: supplied September 17, 2026 as `C:/Users/after/Downloads/Star Background.png`, copied byte-for-byte. The stationary, aspect-preserving full-screen star-map background is drawn first only when zoomed into the regional star map, with one-pixel overscan and a neutral 148/255 brightness tint. It replaces decorative parallax stars. It fades in as the existing galaxy overview fades out; zooming back out restores `deep-field-v3.png`. Nebulae, galaxy gas, markers and labels render above it. No image edits were applied.
