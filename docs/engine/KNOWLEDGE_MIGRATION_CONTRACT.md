# Civilization knowledge authority

Port `Knowledge/CivilizationKnowledgeState.cs` without giving presentation, map clicks, or route queries discovery authority. The native state must distinguish known coordinates, detection, reconnaissance, completed survey, civilization contact, core access and actual core exploration. This state is a dependency of later exploration; a stored fleet route or a visible map marker is not discovery proof.

Retain SystemSurveyLevel values0..3 and complete SystemSurveyKnowledgeView. Provide source-equivalent queries/mutations, sensor reveal, initial knowledge and Snapshot. Snapshot contains only known-system and known-civilization dictionaries; it deliberately omits detailed survey and galactic-core state. Do not call it a complete save format. Expose the other source getters for complete test observation. Preserve source dictionary insertion order in snapshot observer entries and ascending IDs inside their collections. Survey views are sorted by system ID. No getter creates state.

RevealSystem creates Detected/progress0 survey state even when called repeatedly, but does not overwrite existing survey information. Reconnaissance first reveals the system, then returns false when already fully surveyed; otherwise clamp max(oldProgress,floor) to0..0.999999, set Partial and return true for a level change or progress change exceeding1e-7. AdvanceSystemSurvey returns false before revealing anything for delta<=0, otherwise reveals then updates, and reports true only on the transition to Full. MarkFullySurveyed reveals and sets1/Full, returning whether level/progress changed. Preserve exact NaN/Infinity Math semantics in actual operation fixtures; separately hardened save import is not this gate. Do not replace legitimate partial progress with unconditional completion.

IsCivilizationKnown(observer,target) is true for self even when no stored contact exists. RevealCivilization(self) returns false without creating an entry. Other contacts use source set uniqueness. GetKnownCivilizations returns only stored contacts, not synthetic self. Source ordinary knowledge operations accept negative IDs; only UnlockGalacticCoreAccess explicitly rejects negative civilization IDs. Core exploration succeeds only after access unlock and only once; discovered requires both access and exploration. Sorted core observers list access holders, including those who have not explored.

Sensor reveal finds the first matching origin or throws the exact unknown-origin InvalidOperation message. Compute rangeSquared in double from the float sensor input, and compare each physical squared separation in source input order. Negative sensor range is squared like the source, NaN reveals none, infinity covers finite geometry. Count only newly revealed systems, including first origin reveal. Existing native physical-distance input validation remains an explicit finite-geometry boundary. CreateInitial loops civilizations in source order, marks each home fully surveyed, then reveals nearby detected systems; failures do not constitute a returned initialized state.

Native ownership is independent value state; do not reference mutable JSON or retained Godot/C# objects. Prefer ordered observer records with sets/maps for membership and deterministic output. Keep source state creation and dictionary iteration semantics observable without exposing writable internals. Full before/after snapshots in tests include known systems, known civilizations, per-observer survey views and core access/discovery, with a fixed supplied observer universe containing touched and untouched IDs. Source Snapshot is separately tested with its intentionally narrower fields. No campaign tick, save-v16 adapter, survey timer, sensor capability override, UI fog rendering, or hidden-core label is added here.

Verification should use explicit source-authored command sequences, repeat/no-op behavior, reconnaissance-to-survey progression, completed-state stability, zero/negative/nonfinite progress, self-contact, negative-ID distinctions, exact/inside/outside sensor ranges and optional depth, unknown origin, repeated sensor reveal, multiple civilizations, initial home versus surrounding detection, and core unlock-before-explore ordering. Generate actual C# results, catch only simulation calls, parse expected inputs/errors before native operations, and assert complete results/state outside catches. Unknown commands/schema fail rather than being skipped.
