# Stellar Engine migration handoff

Clean engine 0.1.24 is committed as `febf1d7cb42ecd52b017c861376737ed9c0ea183`. Package `Builds/Windows/StellarContinuum-windows-benchmark-febf1d7c-20260913T112624560772Z` verifies `sourceDirty: false`, 61/61 CTest and 20/20 Python checks, seven sealed runtime files and all relocated launch, initialization, recovery and retained campaign simulation checks. The exact export log is `work/native-024-clean.log`. All six remote workflows for this commit passed, including native run `34754320633` and screenshot run `34754320648`. This remains a headless engine checkpoint, not a graphical player build.

Engine 0.1.17 integrates colony commands and timed establishment (88 forwarding cases, 36 actual C# runtime sequences, four native boundaries), strategic intent/provider replay, observer-local strategic planning (75 source cases, 54 commands), and read-only combat readiness/prototype capabilities (56+22 source cases). It also repairs settlement population formatting with two actual-source regressions. See each subsystem's validation document for fixture hashes, source-only null observations and limitations.

Engine 0.1.18 integrates strategic own-state construction (035), matched combat commands (036), the retained strategic director/runtime (037), ordered campaign stepping and combat outcomes (038), and exact-own fleet status (039). The final tightened campaign consumer passed focused strict Debug and Release replays and the complete clean export checks for that checkpoint. The coordinator queries capabilities at their original point of use, retains one shared callable, and reconstructs borrowed views after ship/colony append. It preserves the legacy research path and does not yet migrate Adaptive Research.

Engine 0.1.19 integrates the maintained multi-step campaign benchmark (041), deterministic diagnostic output, no-overwrite protection, relocated simulation checks and four-size export measurements. Development validation passed 49/49 CTest and 20/20 Python checks in `work/native-041-campaign-benchmark-testing.log`. Longer runs at 250 and 2500 systems each completed 1000 steps across two repeats, produced allocation/construction output and matched final state exactly; see CAMPAIGN_SIMULATION_BENCHMARK_VALIDATION.md. The historical exact committed export is recorded in `work/native-019-clean.log`.

Engine 0.1.20 integrates remaining coordinator commands (040): military orders/previews/deployment, own status, colony/outpost plans and orders, freight, and civilian recovery. The retained fixture covers eight actual C# scenarios and 42 sequential commands, plus two raw null-order observations and a separately counted native revision-exhaustion probe. Nonempty plans, successful settlement orders, duplicate-ID re-resolution and partial failures compare complete native state/results. The local export of commit `0567f3d3ef33fa5d486800db7c002cada24aa2c0` passed 50/50 CTest and 20/20 Python checks, seven-file sealing and relocated campaign simulation. Package `StellarContinuum-windows-benchmark-0567f3d3-20260913T083837965101Z` has `sourceDirty: true`: a gate043 draft compiler emitted a root PDB during export. It was identified and moved into its ignored draft directory; the package remains a development export, not clean evidence, despite the historical log filename `work/native-020-clean.log`.

The same commit's native GitHub job `103699709095` failed because cold compilation exceeded the exporter default of 300 seconds. Compilation now receives a bounded 900 seconds and the workflow 25 minutes, while short runtime checks keep their existing limits. Do not describe 0.1.20 CI as green. The clean 0.1.21 local export supersedes its development package; its remote native run also passed.

Engine 0.1.21 integrates the immutable Adaptive Research catalog (042), facility/progress catalogs (043), and sparse civilization state (044), plus the cold-compile timeout repair. Source comparisons cover 103 catalog cases with three separate source-only observations; 28 policy cases with 2,969 facility queries and exact input-byte fingerprints; and 74 state commands with seven constructor cases. State preserves sparse-slot enumeration, revisions, contextual evidence, paused labs, Unicode whitespace and UTF-16 ordinal property ordering. Mutations rebuild only their affected read caches. Its exact committed export verified engine version 0.1.21 and all 53/20 checks in `work/native-021-clean.log`.

Engine 0.1.22 integrates applicability (045), eligibility (047), and standalone research snapshots (046). Applicability checks 26 actual source rows and exact input bytes; eligibility checks 415 cases across 370 canonical nodes and all 19 blocker codes, plus six separate source-only null cases; snapshots replay 73 source records plus one separately evidenced null-string boundary. The combined maintained build passed 56/56 CTest and 20/20 Python checks in `work/native-046-snapshot-testing.log`. The exact committed 0.1.22 export also passed all 56/20 checks and verified its clean manifest.

The schema-1 research codec is not campaign save-v16. It preserves normal serialized payloads and tested semantic restore behavior; a source-accepted null non-nullable string is rejected explicitly, and arbitrary combined malformed JSON does not have universal error-order parity. See the scoped validation documents for exact hashes, failures, ownership and test limits.

Engine 0.1.23 integrates view generation (048), runtime kernel (049), and expertise catalog/state (050). View evidence contains 20 actual-source cases and one separate null-state observation. Runtime evidence contains 63 commands across 20 scenarios, all 14 event types, full state after rejections/failures, nonfinite formatting/progress cases, and native ownership/alias regressions. Expertise evidence contains all 36 fields and 24 institutions, four vector queries, 35 state commands, and 26 malformed loads replayed from retained exact input bytes. The combined maintained build passed 59/59 CTest and 20/20 Python checks. Its exact committed clean export also passed all 59/20 checks. The earlier 0.1.23 package ending `20260913T104243841856Z` recorded `sourceDirty: true` after a temporary documentation edit during packaging; that development run is retained in `work/native-023-development.log` and is not clean export evidence.

Authority and AuthorityInputs (053) are integrated and passed their focused maintained replay in `work/native-053-authority-focused.log`: all eight profiles, 29 sequential commands, and native move/alias checks. An 80-year advance crosses research stages through maturity with matching events and state; capacity loss, duplicate evidence recalculation, partial failures and the source paused-hypothesis practice behavior are retained. Schema-2 research recovery (054) is integrated with 81 actual-source cases. Pressure support with weak state identity (055) and visible-only agenda planning (056) continue in isolated drafts. Follow SOURCE_PARITY_ACCEPTANCE.md and ADAPTIVE_RESEARCH_MIGRATION_CONTRACT.md. Integrated Adaptive Research, diplomacy, save-v16 persistence and SDL3/Vulkan presentation remain open.

PR #325 remains a draft against integration. No integration/main merge or Godot removal is authorized before full parity. The existing Godot 0.1.7 Alpha remains the playable baseline. Territorial PR #323 is paused with enclosed pockets unresolved. No native graphical, player-save, playable-runtime or FPS parity claim applies.

Gate 051 readiness/service and owned expertise state attachment have passed the maintained 60/60 CTest and 20/20 Python suite (`work/native-051-readiness-testing.log`). Source evidence is 23 readiness cases, 36 sequential service commands and six focused service cases. Gate 052 starting profiles is integrated and passed its focused 1/1 CTest check; source evidence covers all eight canonical profiles and 42 boundary rows with exact retained input bytes. These gates form engine 0.1.24. Its exact committed clean export passed the combined 61/20 checks; native CI run `34754320633` also passed. The newer 053 focused evidence does not change that committed package's contents.

Engine 0.1.25 integrates calculated research authority/inputs (053) and standalone schema-2 expertise recovery (054). The combined maintained suite passed 63/63 CTest and 20/20 Python checks (`work/native-054-recovery-testing.log`). The initial link attempt omitted the Windows bcrypt dependency used by the snapshot fixture hash check; the complete Windows-only test target now links it, and the retained initial failure is `work/native-054-recovery-link-failure.log`. The exact committed 0.1.25 export is the next release check; do not describe it as complete until its manifest and relocation report are verified.
