# Campaign massive combat begin/reconcile parity

Gate 094 established authoritative `CampaignMassiveCombat.Begin` and `Reconcile` on the canonical `FreshCampaignState::active_combat_encounter`. Its retained regression remains in place. Gate 095 extends the same service with `Advance`, observer, engagement evidence and doctrine; see `CAMPAIGN_MASSIVE_LIFECYCLE_VALIDATION.md`. Neither gate establishes player-session clock routing.

The retained 23-row actual-source fixture compares complete fleet and encounter state before and after every operation plus exact results or typed errors. It covers constructor null rejection; Begin validation and partial combat-state materialization; separately allocated equal loadouts; scalar and signed-zero loadout identity differences; nonfinite loadout and vessel serialization; active-hostility and absent/repeated reconciliation; partial pools and important-vessel damage; NaN pool propagation; clone failure order; checked important-vessel ID conversion; population and travel cleanup; 130 destroyed fleets with the 128 destruction-event cap and final engagement event; and Begin, detached encounter clone, one engine step, and reconciliation.

Source serializes the loadout identity before cloning `TacticalVessel`. The combined nonfinite row has the same exception text and observable mutation whichever serializer fails first, so ordering is established by direct source/code audit; the row proves the matching externally observable failure. Separate loadout-only and vessel-only rows prove both typed failure paths.

The maintained generator is `tests/Stellar.CampaignMassiveCombat.ParityGenerator`. Its actual `Game.csproj` reference regenerated the fixture byte for byte after integration; SHA-256 is `66522A770C73610607C30E84EC2E69D26C7F10B198E68D1638D6387422F09EEA`. The focused maintained CTest `campaign_massive_combat_parity` passed (`work/native-094-integration.log`). Native Debug and Release use `/W4 /WX /permissive- /fp:precise /utf-8`, pass 23/23 rows, verify the fixture and five source fingerprints before and after replay, and retain explicit object/PDB paths. Missing arguments, fixture and source root all return nonzero with useful diagnostics.

Native-only ownership probes move-construct and move-assign the Pimpl service, then invoke `Begin` through the moved instances to prove the owned hostility callable remains live. The constructor probe also checks the exact `hostility` argument name. The external generator's missing-argument and missing-source-root paths likewise return nonzero diagnostics.
