# Gate 061 validation

The retained fixture contains 53 actual-source rows: 8 canonical profile round trips, 2 top-level schema 1/2 fallbacks, 21 typed restores, 13 JSON deserialize cases, 7 explicit null-record boundaries, 1 duplicate scalar property case, and 1 nonfinite review capture case. Every production call records the canonical research-directory fingerprint before and after; native replay verifies the same fingerprint after loading and after every call.

- Canonical research fingerprint: `2E76D70C9E270E69DF4BFC66B4135419720A0BA5CCA0B2D61DD5906FA22975B1`
- Fixture SHA-256: `6F196AF14BCC96BBBC107BDEBAA0B36F6F57A2B0B99FA5B717711781A5157E40`
- Retained generator SHA-256: `8B745C272239768755954F6E233B4B72E61E3D862CDCC5CC21C9686A1FE72900`
- Production source SHA-256: `4A449083EB4A740FD70FD82FF67759CE7AC083F821748739B162F6B269E41D5D`
- Public header SHA-256: `806784EE8A930B3D495C4459B67A0E813428EE59171D94C3CD1C6FC6E07AA22F`

The fixture regenerated byte-for-byte with exit 0. Running the retained generator through `dotnet run` with no arguments and with a missing research root both exited 1 and printed the full exception, current directory, research-root argument and fixture argument to the terminal. Logs are retained as `managed-missing-args.log` and `managed-missing-root.log`.

Both isolated MSVC Debug (`/Od /RTC1`) and Release (`/O2 /DNDEBUG`) builds passed `/std:c++latest /W4 /WX /permissive- /fp:precise /utf-8`. Each configuration passed all 53 schema 3 source rows, the retained 81-row schema 2 expertise snapshot replay, and the maintained 73-row schema 1 snapshot replay. Compiler outputs use explicit ignored `/Fo` and `/Fd` paths below `work/061-research-strategic-snapshot/build-debug` and `build-release`.

The source rows establish exact normal schema 3 serialization bytes, all eight composed profile states, restored revisions and support, next Pressure targets and Agenda shortlist behavior, V1/V2 fallback activation, nested schema 1/default 0 rejection, typed semantic messages, insertion-order restore, and nonfinite last-review handling. Successful typed restores compare exact reserialization plus state, materialized-view, expertise, every node, every active-project, Pressure-support and Agenda revisions; they also replay the next Pressure target and Agenda shortlist. Nested schema 1/default 0 and the authored missing outer schema compare exact mapped error type and message.

Native JSON syntax and shape failures use `AdaptiveResearchSnapshotJsonError`; nlohmann diagnostic text is not claimed to match System.Text.Json text. Missing or null `Research`, `PressureSupport`, `Agenda`, their dictionary/list members, or `Orientations` can produce source null-reference failures that cannot be represented by the non-null owning native DTOs; the fixture records these separately and requires a stable native JSON rejection. The DTO decoder processes declared members in a fixed order, so compound malformed payloads with multiple faults in a different property order can report a different first parser error. No universal malformed-error precedence or arbitrary floating-number byte spelling is claimed beyond the retained rows.

## Maintained integration

The initial combined Engine 0.1.28 build passed 70/70 CTest and 20/20 Python checks. After the final fixture/assertion and nullable V2 DTO corrections, all five affected maintained checks passed (`work/native-028-final-focused.log`): schema1, schema2, discovery, owning facade and schema3. Exact committed export `a9610c7426d8fdf5c6f493a2b28b154fe7081b6f` passed all 70/70 CTest and 20/20 Python checks. Package `Builds/Windows/StellarContinuum-windows-benchmark-a9610c74-20260913T144010986128Z` has `sourceDirty=false`, seven hashed runtime files and all nine relocation/recovery flags true (`work/native-028-clean.log`). Separate-machine certification remains open. The console oracle is maintained in `tests/Stellar.AdaptiveResearchStrategicSnapshot.ParityGenerator`.

The nonfinite Capture boundary uses an internal fixture-only metadata setter corresponding to the retained C# reflection probe. Public gameplay and Restore continue through the validated agenda writer.
